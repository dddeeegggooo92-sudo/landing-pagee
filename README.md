# Landing Page

Landing page estatica para curso de chino mandarin.

## Uso local

Abre `index.html` directamente en el navegador, o ejecuta:

```bash
node static-server.mjs
```

Luego visita `http://127.0.0.1:4173/`.
